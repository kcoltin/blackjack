/*
 * boolean.h
 * Kevin Coltin  
 *
 * Simply defines values of true and false. The reason this is a separate file 
 * is so that they can be included as constants (rather than defined macros) in
 * multiple headers in the same program without clashing. 
 */

#ifndef BOOLEAN_H 
#define BOOLEAN_H

extern const int TRUE;
extern const int FALSE;

#endif 
